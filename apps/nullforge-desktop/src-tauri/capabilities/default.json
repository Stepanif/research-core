{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the launch-only NullForge shell.",
  "windows": ["main"],
  "permissions": []
}
